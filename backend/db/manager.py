"""
manager.py — Layer 2 (NOT YET IMPLEMENTED).

Registry of known databases ("tabs") + open/create/import/export.
See CLAUDE.md > "Multi-database / tabs model" and "Export / import".
Stores only non-secret pointers (display name, path, last_opened).
Never stores passphrases or keys.
"""
